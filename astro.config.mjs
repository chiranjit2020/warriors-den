import { defineConfig } from 'astro/config';

// Site URL is a placeholder until the final domain is confirmed
// (see docs/research.md §9, item "Domain").
export default defineConfig({
  site: 'https://denmma.netlify.app',
});
